/**
 * Worker-compatible filesystem access
 *
 * This module provides standalone functions for accessing OPFS and IndexedDB
 * from Web Workers that can't use ES modules (classic workers).
 *
 * Usage in a classic worker:
 *   importScripts('/path/to/siglum-filesystem-worker.js')
 *   const data = await siglumFS.readBinaryOPFS('/path/to/file')
 *
 * The compiled output is a self-contained IIFE that exposes `siglumFS` globally.
 */
declare const IDB_NAME = "siglum_filesystem";
declare const IDB_VERSION = 1;
declare const IDB_FILES_STORE = "files";
/**
 * Read binary data from IndexedDB
 */
export declare function readBinaryIDB(path: string): Promise<Uint8Array | null>;
/**
 * Write binary data to IndexedDB
 */
export declare function writeBinaryIDB(path: string, content: Uint8Array): Promise<void>;
/**
 * Check if a file exists in IndexedDB
 */
export declare function existsIDB(path: string): Promise<boolean>;
/**
 * Read multiple files from IndexedDB in a single transaction
 */
export declare function readBinaryBatchIDB(paths: string[]): Promise<Map<string, Uint8Array>>;
/**
 * Read binary data from OPFS
 */
export declare function readBinaryOPFS(path: string): Promise<Uint8Array | null>;
/**
 * Write binary data to OPFS
 */
export declare function writeBinaryOPFS(path: string, content: Uint8Array): Promise<void>;
/**
 * Check if a file exists in OPFS
 */
export declare function existsOPFS(path: string): Promise<boolean>;
/**
 * Read multiple files from OPFS in parallel
 */
export declare function readBinaryBatchOPFS(paths: string[]): Promise<Map<string, Uint8Array>>;
/**
 * Check if OPFS is available
 */
export declare function isOPFSAvailable(): boolean;
/**
 * Read binary data, trying OPFS first then IndexedDB
 */
export declare function readBinary(path: string): Promise<Uint8Array | null>;
/**
 * Write binary data, trying OPFS first then IndexedDB
 */
export declare function writeBinary(path: string, content: Uint8Array): Promise<void>;
/**
 * Check if file exists, trying OPFS first then IndexedDB
 */
export declare function exists(path: string): Promise<boolean>;
export { IDB_NAME, IDB_VERSION, IDB_FILES_STORE };
//# sourceMappingURL=worker.d.ts.map